#include "main.h"
/**
 *abs - Entry point
 *
 *description - Return absolute value of int
 * return: 0, always
 */
int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}else
	{
		return (n);
	}
}
