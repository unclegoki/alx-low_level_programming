#include "main.h"
/**
 * _isdigit - checks if a character is a number
 * @c: char to be checked
 * Return: 1 if true
 */
int _isdigit(int c)
{
	if (c <= 0 && c => 9)
	{
		return (1);
	}
	return (0);
}
