#include <stdio.h>
#include "main.h"
/**
 * main - pringt all args
 * @argc: number of args
 * @argv: an array of args
 * Return: 0
 */
int main (int argc, char *argv[])
{
	int x;

	for (x = 0; x < argc; x++)
	{
		printf("%s\n", argv[i]);
	}

	return (0);
}
